<Window x:Name="window" x:Class="SEACC_WPFControls.SEACC_LoggingOffBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         Title="Messege Box" Topmost="True" Loaded="window_Loaded" WindowStyle="None"   WindowStartupLocation="CenterScreen"  AllowsTransparency="True" BorderThickness="0"  PreviewKeyDown="window_PreviewKeyDown" Background="#66383838" Height="218.966" Width="630.3"  >
    <Window.Resources>
        <Style x:Key="iButton" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid x:Name="LayoutRoot" Background="{TemplateBinding Background}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="MouseOver">
                                        <Storyboard>
                                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)" Storyboard.TargetName="grid">
                                                <EasingColorKeyFrame KeyTime="0" Value="#00C7C7C7"/>
                                                <EasingColorKeyFrame KeyTime="0:0:0.3" Value="#4CC7C7C7"/>
                                            </ColorAnimationUsingKeyFrames>
                                        </Storyboard>

                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ColorAnimationUsingKeyFrames Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)" Storyboard.TargetName="grid">
                                                <EasingColorKeyFrame KeyTime="0" Value="#4CC7C7C7"/>
                                                <EasingColorKeyFrame KeyTime="0:0:0.1" Value="#99C7C7C7"/>
                                            </ColorAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid x:Name="grid" Background="#00C7C7C7">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Grid>
                            </Border>

                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True"/>
                            <Trigger Property="IsDefaulted" Value="True"/>
                            <Trigger Property="IsMouseOver" Value="True"/>
                            <Trigger Property="IsPressed" Value="True"/>
                            <Trigger Property="IsEnabled" Value="False"/>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </Window.Resources>

    <Grid x:Name="grid" Width="600"  VerticalAlignment="Center" Background="White" MinHeight="170">
        <Grid.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="50" Color="{Binding MessegeBox_Background, ElementName=window}" Opacity="0.5"/>
        </Grid.Effect>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>

        <Grid x:Name="grdHeader" Grid.Row="0" Background="#FF4791C3">
            <TextBlock x:Name="lbl_Caption" Text="System is Going to Logging Off Now..." FontSize="28" Foreground="White" FontWeight="ExtraLight" TextWrapping="Wrap" Margin="20,8,20,0" />
        </Grid>

        <StackPanel Grid.Row="3" Width="{Binding ActualWidth, ElementName=grid1, Mode=OneWay}" Orientation="Horizontal" FlowDirection="RightToLeft" >
            <Button x:Name="Btn_OK" Style="{StaticResource iButton}"  Content="Stop Logging Off"  Click="Btn_OK_Click" Height="27" Width="110" Margin="11,0,1,0" Background="#19000000" BorderBrush="White" Foreground="White"    />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBox Name="tbTime" Margin="20,10,20,10" FontSize="16" VerticalAlignment="Center" IsReadOnly="True" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" BorderBrush="{x:Null}" ></TextBox>
            <TextBox Name="lblMessege" Margin="20,10,20,10" Text="Do you need to stop logging off now?" VerticalAlignment="Center" IsReadOnly="True" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" BorderBrush="{x:Null}" ></TextBox>
        </StackPanel>
    </Grid>
</Window>