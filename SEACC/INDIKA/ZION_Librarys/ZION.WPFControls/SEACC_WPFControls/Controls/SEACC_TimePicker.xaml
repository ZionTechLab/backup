<UserControl x:Name="userControl" x:Class="SEACC_WPFControls.SEACC_TimePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Background="{Binding BackgroudColor, ElementName=userControl}" MouseLeftButtonUp="userControl_MouseLeftButtonUp">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Grid x:Name="grdDate"  Visibility="{Binding ShowDate, ElementName=userControl}" MouseLeftButtonUp="grdDate_MouseLeftButtonUp" MouseRightButtonUp="grdDate_MouseRightButtonUp" >
                <StackPanel Orientation="Horizontal" >
                    <TextBlock x:Name="tbYear" Text="2015" Foreground="{Binding FontColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0,0,0" MouseLeftButtonUp="grdDate_MouseLeftButtonUp" />
                    <TextBlock Text="/"  Foreground="{Binding SeperatorColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0" MouseLeftButtonUp="grdDate_MouseLeftButtonUp" />
                    <TextBlock x:Name="tbMonth" Text="02" Foreground="{Binding FontColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" MouseLeftButtonUp="grdDate_MouseLeftButtonUp" />
                    <TextBlock Text="/"  Foreground="{Binding SeperatorColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0" MouseLeftButtonUp="grdDate_MouseLeftButtonUp" />
                    <TextBlock x:Name="tbDay" Text="05" Foreground="{Binding FontColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,5,0" MouseLeftButtonUp="grdDate_MouseLeftButtonUp" />
                </StackPanel>
                <TextBox x:Name="txtDate" Visibility="Hidden" LostFocus="txtDate_LostFocus" Margin="2" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="15" KeyUp="txtDate_KeyUp" Padding="0"></TextBox>
            </Grid>
            <Grid x:Name="grdTime" Visibility="{Binding ShowTime, ElementName=userControl}" MouseRightButtonUp="grdTime_MouseRightButtonUp" MouseLeftButtonUp="grdTime_MouseLeftButtonUp" >
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="tb_Hours" Text="01" Foreground="{Binding FontColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10,0,0,0" MouseLeftButtonUp="grdTime_MouseLeftButtonUp" />
                    <TextBlock  Text=":" Foreground="{Binding SeperatorColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="2,0" MouseLeftButtonUp="grdTime_MouseLeftButtonUp" />
                    <TextBlock x:Name="tb_Miniths" Text="01" Foreground="{Binding FontColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0,0,5,0" MouseLeftButtonUp="grdTime_MouseLeftButtonUp" />
                    <TextBlock Text="H" Foreground="{Binding FontColor, ElementName=userControl}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0,0,5,0" MouseLeftButtonUp="grdTime_MouseLeftButtonUp" />
                </StackPanel>
                <TextBox x:Name="txt1" Visibility="Hidden" LostFocus="txt1_LostFocus" Margin="2" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"  KeyUp="txt1_KeyUp" Padding="0"></TextBox>
            </Grid>
        </StackPanel>
        <Popup x:Name="pop_lists" IsOpen="False" StaysOpen="False" Placement="Bottom" PlacementTarget="{Binding ElementName=grd_TextBox}" AllowsTransparency="True" Closed="pop_lists_Closed"  >
            <Border BorderThickness=".5"  Background="#FF727272" >
                <Border.Effect>
                    <DropShadowEffect/>
                </Border.Effect>
                <Grid x:Name="grd_List" Visibility="Visible" Width="{Binding PopUpWidth, ElementName=userControl}" MaxWidth="220" Margin="2" KeyUp="grd_List_KeyUp" >
                    <StackPanel Orientation="Horizontal">
                        <ListBox x:Name="lb_years"  HorizontalAlignment="Left" Height="100" VerticalAlignment="Top"  ScrollViewer.VerticalScrollBarVisibility="Hidden" Visibility="{Binding ShowDate, ElementName=userControl}" SelectionChanged="lb_years_SelectionChanged"/>
                        <ListBox x:Name="lb_months"  HorizontalAlignment="Left" Height="100" VerticalAlignment="Top"  ScrollViewer.VerticalScrollBarVisibility="Hidden" Visibility="{Binding ShowDate, ElementName=userControl}" SelectionChanged="lb_months_SelectionChanged"/>
                        <ListBox x:Name="lb_dates"  HorizontalAlignment="Left" Height="100" VerticalAlignment="Top"  ScrollViewer.VerticalScrollBarVisibility="Hidden" Visibility="{Binding ShowDate, ElementName=userControl}" SelectionChanged="lb_dates_SelectionChanged" />
                        <ListBox x:Name="lb_Splitter"  HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="2" ScrollViewer.VerticalScrollBarVisibility="Hidden" Visibility="Hidden" SelectionChanged="lb_dates_SelectionChanged" />
                        <ListBox x:Name="lb_hours"  HorizontalAlignment="Left" Height="100" VerticalAlignment="Top"  ScrollViewer.VerticalScrollBarVisibility="Hidden" Visibility="{Binding ShowTime, ElementName=userControl}" SelectionChanged="lb_hours_SelectionChanged"/>
                        <ListBox x:Name="lb_mins"  HorizontalAlignment="Left" Height="100" VerticalAlignment="Top"  ScrollViewer.VerticalScrollBarVisibility="Hidden" Visibility="{Binding ShowTime, ElementName=userControl}" SelectionChanged="lb_mins_SelectionChanged"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl>