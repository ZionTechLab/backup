<UserControl x:Class="Digiteq.UC_CalanderDate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" MouseUp="UserControl_MouseUp"  >
    <UserControl.Resources>
        <Storyboard x:Key="OnMouseEnter1">
            <ThicknessAnimationUsingKeyFrames Storyboard.TargetProperty="(Border.BorderThickness)" Storyboard.TargetName="border">
                <EasingThicknessKeyFrame KeyTime="0" Value="2"/>
            </ThicknessAnimationUsingKeyFrames>
        </Storyboard>
        <Storyboard x:Key="OnMouseLeave1">
            <ThicknessAnimationUsingKeyFrames Storyboard.TargetProperty="(Border.BorderThickness)" Storyboard.TargetName="border">
                <EasingThicknessKeyFrame KeyTime="0" Value="0"/>
            </ThicknessAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Mouse.MouseEnter">
            <BeginStoryboard Storyboard="{StaticResource OnMouseEnter1}"/>
        </EventTrigger>
        <EventTrigger RoutedEvent="Mouse.MouseLeave">
            <BeginStoryboard x:Name="OnMouseLeave1_BeginStoryboard" Storyboard="{StaticResource OnMouseLeave1}"/>
        </EventTrigger>
    </UserControl.Triggers>
    <Grid Margin="0" >
        <Border  BorderBrush="#FF491313" BorderThickness="0,0,0.2,0.2">

        </Border>
        <Border x:Name="border" BorderBrush="#FF2490CE">
            <Grid>
                <Border x:Name="holyday_Bank" MinWidth="2" MinHeight="2" />
                <Border x:Name="holyday_Company"   BorderThickness="0" Margin="4" MinWidth="2" MinHeight="2" />
                <Border x:Name="holyday_Public"  Margin="0" BorderThickness="2" MinWidth="2" MinHeight="2" />
                <Border x:Name="holyday_poya" CornerRadius="90" Margin="2" BorderThickness="2" MinWidth="2" MinHeight="2" />

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition  Height="*"/>
                        <RowDefinition x:Name="rd_Descript"  Height="0"/>
                    </Grid.RowDefinitions>
                    <TextBlock  Name="txt_Day" Grid.Row="0" Text="Sun" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"  Foreground="#FFFFB7B7" />
                    <TextBlock Name="txt_Day_Descript"  Grid.Row="1"  HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"  Foreground="#FFFFB7B7" />
                </Grid>

            </Grid>
        </Border>
    </Grid>
</UserControl>
